# Case Cards

Store deconstructed reusable cases here. This directory may be empty in V1.

Recommended naming:

```text
YYYYMMDD-short-case-name.md
```

Create a scaffold:

```bash
python scripts/scaffold_case.py --raw path/to/raw_prompt.md --name "case name" --family "action-mechanical" --mode video
```
